import React from 'react';
import Navigation from '../composant/Navigation';

const Compte = (props) => {
    return (
        <div>
            <Navigation/>
            <h1>compte</h1>
            
        </div>
    );
};

export default Compte;